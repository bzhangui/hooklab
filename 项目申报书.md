# HookLab 项目申报书

## 基本信息

- **项目名称：** HookLab
- **GitHub 仓库：** https://github.com/bzhangui/hooklab
- **项目方向：** MoonBit Webhook 安全验证与可靠事件交付基础库
- **是否为原创项目：** 是

## 项目简介

HookLab 是一个使用 MoonBit 开发的跨平台 Webhook 安全与可靠性交付工具。项目面向支付回调、代码托管事件、协同办公通知和自动化任务等场景，解决第三方回调中常见的原始请求体验签失败、旧请求重放、重复事件产生多次副作用、下游暂时不可用以及调试信息泄露敏感数据等问题。项目计划提供可嵌入的 MoonBit 核心库和易于使用的命令行工具，让开发者能够验证、诊断、路由和安全重放 Webhook，并获得稳定、可复现的处理结果。

## 项目方向与适用场景

- 为 MoonBit 服务端应用提供统一的 Webhook 接收、验签和事件处理基础能力；
- 为支付、GitHub、飞书等第三方回调提供防重放、幂等和故障诊断；
- 为自动化平台、任务流水线和事件驱动应用提供可靠投递、重试与死信处理；
- 为本地开发和测试提供脱敏后的离线报告与可复现样例。

## 拟实现的核心功能

- 提供统一的 Webhook 请求与验证结果模型，支持 GitHub、Stripe、飞书/Lark 和通用 HMAC-SHA256 协议；
- 使用 MoonBit 实现 SHA-256、HMAC-SHA256、签名格式校验和常量时间摘要比较，并逐步完善字节流式处理；
- 支持时间窗口检查、delivery ID 幂等、防重放策略和可替换的持久化存储接口；
- 支持 JSON 路径规则路由、嵌套敏感字段脱敏、机器可读诊断结果和单文件离线报告；
- 支持确定性指数退避、错误分类、有限重试、持久化任务队列和死信恢复；
- 提供 `sign`、`verify`、`report`、`route-test`、`retry-plan`、`replay` 等命令，并保持 JS、Wasm、Wasm-GC 和 Native 多目标构建与测试；
- 提供快速开始、协议说明、安全边界、示例和自动化测试，保证关键流程可构建、可运行、可复现。

## 原创、移植或参考说明

HookLab 是原创 MoonBit 项目，不是对现有项目的直接移植。项目的协议行为参考 GitHub、Stripe、飞书官方 Webhook 文档以及 SHA-256、HMAC 标准测试向量，但未复制相关 SDK 或其他开源项目的源代码。整体架构根据 MoonBit 的类型系统、包结构和多后端编译能力重新设计，核心代码目前不依赖第三方运行时包；参考资料和协议来源记录在仓库的 `THIRD_PARTY_NOTICES.md` 中。
